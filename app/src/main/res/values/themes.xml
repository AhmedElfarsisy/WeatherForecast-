<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WeatherForcast" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gray_background</item>
        <item name="colorPrimaryVariant">@color/dark_gray</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_yellow</item>
        <item name="colorSecondaryVariant">@color/main_yellow</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.WeatherForcast.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="itemBackground">@color/gray_background</item>
    </style>

    <style name="Theme.WeatherForcast.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="cornerRadius">4dp</item>
    </style>

    <style name="Theme.WeatherForcast.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>